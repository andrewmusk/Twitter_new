<% provide(:title, @title) %>
<div class="row">
  <section class="user_info">
    <h1><%= @user.name %></h1>
  </section>
  <section class="stats">
    <% if @users.any? %>
      <div class="user_avatars">
        <% @users.each do |user| %>
          <%= user.name %>
        <% end %>
      </div>
    <% end %>
  </section>
</aside>
<div class="col-md-8">
<%= link_to "Back", :back %>